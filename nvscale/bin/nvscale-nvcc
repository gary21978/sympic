#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
NVCC="${NVCC:-nvcc}"

exec "${NVCC}" \
  -x cu \
  --expt-relaxed-constexpr \
  --extended-lambda \
  -I"${ROOT_DIR}/include" \
  "$@"

